# CRM Scalability Upgrade — Program of Record (items 6–15)

Branch: `upgrade/phases-6-15` · target: 3,000 users / 500k customer base · **strangler-fig, no big-bang, nothing ships to live without approval.**

This is a multi-session program. Each phase ships behind a flag and is independently revertible. Status legend: ✅ done & verified · 🟡 scaffolded (ready to wire) · ⬜ planned.

---

## ✅ STATUS 2026-06-15 (SW-32): scaling COMPLETE + #1/#2 done + LIVE-VERIFIED via `@go`
~16 increments shipped live (SW-17→SW-32): all hot-path whole-table reads fixed, permission-scoping (#1) ON + verified, lookups (#2) done. **`@go crm "make sure no bug"` verification (admin browser session):** app healthy (real data renders, React island `ready`, reads OK, "LIVE" indicator green); the React `defineProperty:null` errors were STALE console history (live = es2022 `?v=-15`, no current crash). **Found + fixed one real bug — a stuck FALSE "offline mode" banner** (set on a transient cold-boot/SW blip; never cleared once reads shifted to query/queryAdvanced/queryPaged/BFF): SW-31 clears it on any successful server read (live-verified), SW-32 adds a 4s self-healing poll for BFF/RPC-only screens. **Banner confirmed gone; screen clean.** Pre-existing admin-only console noise remains (`checkForSecurityIncidents`/`monitorLoginAttempts` — non-fatal, not user-facing) — optional future cleanup. React UI migration (#4) still PAUSED per user; test-DB (#3) still user-gated.

### NEEDS USER (to resume the remaining work)
1. ✅ **DONE + VERIFIED LIVE (SW-29, 2f32f48):** `getVisibleProspects/Customers` scoping ON by default (provable parity + auto-fallback) with kill-switch (`?novis=1` · `localStorage.crm_visibility_off='1'` · `window.__SERVER_VISIBILITY=false`). **Verified in the admin browser session 2026-06-15:** for the 5 agents owning the most records, scoped server query counts == legacy filter counts exactly (prospects 116/98/42/34/22, customers 1/0/8/18/0; ok:true, 384 prospects / 164 customers total). No leak, no missing rows.
2. ✅ **DONE (SW-30) — no new DDL needed:** WhatsApp phone attribution (whatsapp:483/512) + pipeline referrer-name (pipeline:286) now resolve via the existing `import_existing_matches` RPC + `searchCustomers` trigram (whole-table fallback on each). **Verified against live data** (both resolved the correct entity by phone / by name). The customers `name` column doesn't exist — that legacy check was a no-op (dropped). **CPS duplicate-block (activities:3755) intentionally LEFT AS-IS** — it's a creation-*blocking* path that works correctly and is fast at the current scale (~548 contacts); revisit only when the contact base is large, with a dedicated tested RPC that returns full rows incl. agent id.
3. **Seed a TEST Supabase project** (env: E2E_EMAIL/E2E_PASSWORD/E2E_BASE_URL) → unblocks the Playwright e2e suite.
4. **React UI migration** (Phase 2/3 islands) — each rebuilds the shared `react-dist/react-island.js` that is LIVE-DEFAULT for customers/prospects, so it must be runtime-verified with a login before shipping; do these with you present (ship each opt-in first).

---

## AUTONOMOUS ROADMAP (2026-06-14, audit-driven — THIS IS THE EXECUTION CHECKLIST)

Source: parallel 5-agent codebase audit → 40 ordered increments. The autonomous loop (and each session) works the TOP unchecked, `deployable`, non-BLOCKED item.
**Rules per increment:** ship behind a flag/kill-switch with a legacy fallback; for scale swaps, guard the new path with `try/catch → legacy` (or default the flag OFF) so a bad path can NEVER break live; verify (`node --check` + `node ci/regression.js` + happy-path) → deploy (`git push origin main` → Vercel; bump `sw.js` CACHE_VERSION only when client bundles change) → check the box + record verification here. Additive DDL pre-authorized; destructive DDL → ask. (Audit ran 2026-06-14; scale-risk file:lines cited inline per item.)

### Phase 1 — remaining full-table reads → server (each isolated + fallback) [S/M/L · risk]
- [x] **sp-marketing-package-customers** (S·low) ✅ SW-17 — `viewPackageCustomers` (marketing:2647) did 3 whole-table reads (purchases+customers+prospects); now: purchases via `query('purchases',{package_id})`, product-name fallback via per-name `query`, member names via bounded `queryAdvanced(scopeField:'id',scopeValues:ids)` IN-lookups. Each wrapped `try/catch → exact legacy getAll path`. Verified: `node --check` + `node ci/regression.js` PASS (build green); fallback-safe by construction.
- [x] **sp-purchases-search** (M·low) ✅ SW-18 — `performTransactionSearch` (search:1363) loaded the WHOLE purchases table. Now pushes provably-equivalent predicates to `queryAdvanced` (payment/status exact eq; product ilike == client includes; date range WIDENED ±1 day → server superset), fetches ≤CAP(10000)+1 in id order, and re-runs EVERY original client filter on the result (identical set). >CAP or any error → exact legacy `getAll` full scan. Results are client-paginated downstream so set/total unchanged. Verified: `node --check` + `node ci/regression.js` PASS.
- [x] **sp-purchases-history-view** (M·low) ✅ SW-19 — (audit mis-described: this view reads converted prospects' `closing_records_history` JSON, NOT the purchases table.) `_loadPurchasesHistory` (prospects:6885) did `getAll('prospects')` (WHOLE table) to collect converted IDs + a 2nd `.in()` query. Now: one server-side `.or('status.eq.converted,conversion_status.eq.approved')` query selecting only the 6 needed cols; `hasLiveSession` guard; any error/offline → exact legacy two-step fallback (getAll + client filter + .in). Rows building/sort/cache unchanged. Verified: `node --check` + `node ci/regression.js` PASS.
- [x] **sp-entity-lookup-rpc** (M·low) ⚠️ PARTIAL ✅ SW-20 — **calendar by-ID done**: `postEventFollowUp` (calendar:5449) dual whole-table scan → `getById('prospects')` then `getById('customers')` (in-memory hit when cached, else single-row fetch) + whole-table fallback; `is_customer` parity preserved. **🚫 BLOCKED (needs additive RPC) — the 3 phone/name sites:** whatsapp:483/512 (find entity by normalized phone — strip `+`/spaces) and activities:3755 (CPS hard/soft duplicate by normalized name+phone+IC) require server-side normalization parity = a new `lookup_entity_by_phone` / `cps_duplicate_check` RPC. Applying DDL needs the Supabase PAT on CLI/file, which the safety classifier blocks in unattended runs → **left on the exact legacy `getAll` path (correct, just not yet scaled)**. Unblock: user applies the RPC, or re-run me where DDL is allowed. Verified calendar fix: `node --check` + `node ci/regression.js` PASS.
- [x] **sp-autocomplete-search** (M·low) ⚠️ PARTIAL ✅ SW-21 — **referrer autocomplete done**: `searchProspectReferrers` (activities:3389) `getAll('prospects')`-per-keystroke → `AppDataStore.searchProspects(term,{includeDormant,limit:100})` (server ilike on name/nickname/phone/email) + reapply exact client filters (active status + name/nickname contains) + slice(5); whole-table fallback on error. (users list kept — ≤22 rows.) **DEFERRED (needs typeahead UI rework, not a source-swap):** `openCustomerReferralModal` (prospects:3196) renders a `<select>` of ALL prospects — capping it would hide valid people, so it needs converting to a search-as-you-type input (do as its own increment or fold into the referral-modal React migration). Verified: `node --check` + `node ci/regression.js` PASS.
- [x] **sp-prospect-activity-indexed** (M·low) ⚠️ MOSTLY DONE ✅ SW-22 — `openMeetupHistoryModal` (calendar:5381) + `openLatestMeetupNotes` (features2:373) whole-`activities` scans → `AppDataStore.getActivitiesForProspect(prospectId)` (server `eq prospect_id`, ordered, limit; **built-in getAll fallback**); client MEETUP_TYPES filter / exact sort reapplied for parity. **DEFERRED:** `getProspectAttendeeNotes` (calendar:5284/5292) scans whole `event_attendees` (by `entity_id||attendee_id`, attendee_type null-tolerant) + whole `activities` for a byId parent map — needs a null-safe `.or(entity_id.eq,attendee_id.eq)` event_attendees query + bounded `.in(id)` parent resolution — ✅ NOW DONE (SW-25): server `.or(entity_id.eq,attendee_id.eq)` superset + exact client trim, + bounded `.in(id)` parent-activity lookup, + whole-table fallbacks. Verified: `node --check` ×2 + `node ci/regression.js` PASS.
- [x] **sp-reassign-cascade-rpc** (M·med) ⚠️ 4/5 DONE ✅ SW-23 — (no new RPC needed — existing `query`/`queryAdvanced` primitives.) Converted: cascadeProspectReassign linked-customers (1111) → `query('customers',{converted_from_prospect_id:pid})`; quickReassign preview count (1613) → eq count query; confirmBulkReassignment matched prospects (1780) → `queryAdvanced(scopeField:'id',scopeValues:checkedIds)` IN; bulk linked-count (1803) → `queryAdvanced(scopeField:'converted_from_prospect_id',scopeValues:selectedIds)` IN. Each reapplies the exact client filter + whole-table fallback. **cascade-activities transfer (1118) — ✅ NOW DONE (SW-26):** `queryAdvanced` scopeFields-OR (prospect_id / customer_id-IN) + lead_agent_id eq, entity-scoped (no server date filter needed — set is tiny), exact client filter (incl. date cutoff) reapplied + completeness guard + whole-table fallback. **sp-reassign-cascade-rpc now 5/5.** Verified: `node --check` + `node ci/regression.js` PASS.
- [x] **sp-visibility-helpers** (L·med) ⚠️ STAGED flag-OFF (2/4) ✅ SW-24 — **getVisibleProspects + getVisibleCustomers** (script.js:740/758) now scope server-side via `AppDataStore.queryPaged({filters:{responsible_agent_id:visibleIds}})` (paged → no 1000-row cap; customers = 2 scoped fetches resp_agent_id OR legacy agent_id, merged+deduped). Behind **`window.__SERVER_VISIBILITY` (DEFAULT-OFF)** + try/catch → exact legacy getAll-then-filter; admin (visibleIds==='all') always getAll. Provable parity (same visibleIds drive `.in()` as client `.includes()`). Regression PASS (flag-off = byte-identical). **⚠️ VERIFY-THEN-FLIP (needs an agent login or admin-session count check): set `window.__SERVER_VISIBILITY=true`, confirm a scoped agent's prospect/customer list count == legacy, THEN default it on.** **DEFERRED (need RPC/view — join/public-OR/co_agents-JSON scoping):** getVisibleReferrals (794, owner resolved via prospect/customer maps), getVisibleActivities (904, public-visible-to-all + co_agents membership). `script.js`,`data.js`
- [ ] **sp-reporting-fallbacks** (M·low) — reporting `getAll('purchases')` FALLBACKs (reporting:285/1830/2078/2101/2130/2163) — bound with a `limit` rather than delete (keep resilience). `chunks/script-reporting.js`
- [ ] **sp-batch-jobs-rpc** (L·med) — weekly inactivity (features2:248), AI lead-score (ai:621), churn (ai:1103), HuiJi migration (pipeline:520) load whole tables → set-based RPC/edge. `features2`,`ai`,`pipeline`,`data.js`
- [ ] **sp-gcal-incremental** (M·med) — gcal:138 `getAll('activities')` → date-window/changed-since cursor. `chunks/script-gcal.js`,`data.js`

**Extra broader-audit scale-swaps (beyond the 11, same eq/IN pattern + fallback):**
- [x] **approval-entry linked-customer lookup** ✅ SW-27 — `approveQueueEntry` (prospects:925 + :943, new_customer/info_update + new_sale) `getAll('customers').find(converted_from_prospect_id==pid)` → `query('customers',{converted_from_prospect_id})` [0] + whole-table fallback (both sites). `node --check` + regression PASS.
- [x] **attendee-modal refresh** ✅ SW-28 — `toggleAttendeeUnattended` (activities:2695) `getAll('activities').filter(event_id&&activity_date)` → `query('activities',{event_id,activity_date})` + exact client re-filter + eventId guard + whole-table fallback. PASS.
- [ ] **pipeline referrer-name lookup** (pipeline:286, Path-B fallback) — `getAll('customers').find(full_name/name===needle)` → BLOCKED-needs-normalization (case-insensitive trimmed match across full_name AND legacy `name` col; same parity hazard as whatsapp/CPS — needs a normalized lookup RPC). Low-freq fallback path; left on legacy getAll.

### Phase 2 — React islands for EASY table/CRUD views (proven pattern + ?react=0 kill-switch)
- [ ] **react-agents-list** (M·low) — Agents/Consultant list → island + `api/agents.mjs`
- [ ] **react-purchases-history** (M·low, blockedBy sp-purchases-history-view) — → island + `api/purchases.mjs`
- [ ] **react-marketing-lists** (M·low) · **react-promotions** (S·low) — marketing CRUD
- [ ] **react-knowledge-hq** (M·low) — Knowledge HQ
- [ ] **react-forms-suite** (M·low) — forms/surveys/contracts/custom_fields (one chunk family)
- [ ] **react-noticeboard** (S·low) · **react-performance-ranking** (M·low)
- [ ] **react-boss-admin-security** (M·low) — read-only L1/L2 dashboards
- [ ] **react-standard-functions** (S·low) · **react-cases** (M·low)
- [ ] **react-protection-view** (M·med, blockedBy sp-reassign-cascade-rpc)

### Phase 3 — HARD views (large; isolate behind flag) + sync teardown
- [ ] **react-calendar** (L·high, blockedBy sp-prospect-activity-indexed) — ~5700 LOC engine
- [ ] **react-pipeline-kanban** (L·high) — ~2950 LOC drag-drop
- [ ] **react-reports-dashboard** (L·high, blockedBy sp-reporting-fallbacks) — Chart.js
- [ ] **react-referrals-tree** (L·high, blockedBy sp-prospect-activity-indexed) — D3
- [ ] **react-ai-insights** (L·high, blockedBy sp-batch-jobs-rpc)
- [ ] **react-fude-milestones** (L·high) · **react-org-chart** (L·high)
- [ ] **react-medium-forms** (L·med, blockedBy sp-purchases-search) — search/import/cps/journey/documents/marketing long tail
- [ ] **refactor-sync-teardown** (L·high, blockedBy react-medium-forms) — data.js SWR/delta/tombstone/offline-queue; ONLY after views stop reading through AppDataStore. Guarded by `tests/contract/data-store.contract.test.mjs`

### Phase 4 — structural cleanup (interleave once surface shrinks)
- [ ] **refactor-core-utils** (S·low) — escapeHtml/formatFileSize/getFileIcon/generateId/convertSolarToLunar → `src/utils` (pure; no flag)
- [ ] **refactor-permissions** (M·low, blockedBy refactor-core-utils) — isAgent/getVisibleUserIds → `src/utils/permissions` (28+ chunks)
- [ ] **refactor-scoring-module** (M·med, blockedBy refactor-permissions) — protection/health scoring → `src/modules/scoring`
- [ ] **refactor-debounce-search** (S·low, blockedBy refactor-core-utils) — autoDebounceAppSearch wrapper → `src/perf`
- [x] **ci-wire-gates** (M·low) ✅ — new `.github/workflows/ci.yml` ("CI Gates") runs `node ci/audit.js` (ghost-call/cross-chunk ReferenceError sweep) + `node ci/test.mjs` (tsc --noEmit scoped + data-layer contract suite) on every PR + push to main. Dependency-free (npx fetches typescript; contract is pure node → no `npm ci`). **REPORTING-ONLY — does NOT gate the Vercel deploy** (so it can never block/break live; a red check just surfaces a regression). Deliberately did NOT add a fatal gate to the Vercel buildCommand (would halt deploys on a flake). Verified both gates green locally before wiring (audit 0 issues; tsc clean + contract PASS). No SW bump (CI-only, no client bundle change). (build+snapshot stays the local pre-push gate.)
- [ ] **build-vite-ownership** (L·high, blockedBy react-medium-forms) — Vite single build owner; retire build.mjs
- [ ] **build-stop-committing-bundles** (M·med, blockedBy build-vite-ownership) — gitignore *.min.js/.br
- [ ] **🚫 BLOCKED test-e2e-suite** — needs USER to provision + seed a separate TEST Supabase project (env: E2E_EMAIL/E2E_PASSWORD/E2E_BASE_URL). Cannot run autonomously; all authed specs `test.skip()` without it.

---

## Phase 0 — Safety net (#10)  ✅
The net that must stay green before any later refactor ships.
- ✅ `tsconfig.json` — incremental `checkJs` over the typed surface (`types/`, `api/`). `tsc --noEmit` is clean.
- ✅ `tests/contract/data-store.contract.test.mjs` — loads `data.js` in a VM and locks the offline-queue/sync classification (incl. the 23503→`fk` bounded-retry fix and `_snapshotsDiffer`). `node --test` → 6/6 pass.
- ✅ `tests/e2e/critical-flows.spec.ts` — Playwright specs for the 8 critical flows; authed specs skip until a **TEST Supabase project** + seeded account is wired (never point at prod data). Some bodies are `test.fixme` pending seed.
- ✅ `ci/test.mjs` — runs tsc + contract suite. (Keep `ci/regression.js` running separately.)
- ⬜ Next: provision a seeded Supabase test project; fill the `fixme` specs; add `npm run test:e2e` to CI.

## Phase 1 — Server-paginated reads (#12)  ✅ customers + prospects
The scale unlock. 255 `getAll('<bigtable>')` full-table fetches → server-side filter/sort/paginate via the existing `queryAdvanced`.
- ✅ Shared **`_serverPage(table, opts)`** helper (`chunks/script-prospects.js`): runs `queryAdvanced` with role-visibility scope injected server-side (`scopeBy:[cols]`), returns `{data,count,used}` or `{used:false}` → caller falls back. (Promote to `_crmUtils`/`lib` when a 3rd chunk adopts it.)
- ✅ **Customers list** (`renderCustomersTable`) behind `window.__SERVER_TABLES` (default OFF → identical legacy behavior). Server-handled: search, `ming_gua`, `house_audit_status`, VIP (`gte lifetime_value`), Agent-Eligible, scope. Client fallback: `Regular` (null edge), `deficiency` (array), `min-events` (aggregation). Fixed unescaped `full_name`/`customer_since`/`ming_gua` XSS.
- ✅ **Prospects list** (`renderProspectsTable`) behind the same flag. Server-handled: search, `ming_gua`, agent, scope, column sort (name/score/activity), paginated. Client fallback: score-grade + protection-status filters (derived, not columns) and protection sort. **Dormancy caveat:** the flagged `queryAdvanced` path is not dormancy-curated (it shows all matching, paginated; never-contacted prospects are correctly NOT hidden) — exact hide-dormant-by-default parity is the job of **`migrations/prospects_page_rpc_2026-06-14.sql`**.
- ✅ **`prospects_page` RPC — APPLIED to live 2026-06-14.** SECURITY DEFINER, does dormancy + scope + filter + sort-ordered page in one call. Corrected against the real schema before applying: params are `bigint` (not `uuid`); no `deleted_at` filter (column doesn't exist — the legacy list filters only by dormancy); search covers full_name/nickname/phone/email; pagination row_number() carries the sort (the scaffold's unordered row_number() paged arbitrarily — fixed). Verified live: 339 active vs 361 all (22 dormant correctly hidden), page boundaries monotonic + non-overlapping, scope 100=100, search 31=31.
- ✅ **Wired into the client.** `AppDataStore.prospectsPage()` (data.js) calls the RPC with the `hasLiveSession()` guard; `_serverProspectsPage()` (chunks/script-prospects.js) resolves role-visibility scope to a `bigint[]` and routes `renderProspectsTable`'s flagged server path through it (replacing the dormancy-blind `_serverPage`/`queryAdvanced` call). Agent-filter / include-dormant toggle force include-dormant, mirroring legacy `includeDormant:!!agentFilter`. On any guard/RPC error → `{used:false}` → legacy client path. Still behind `window.__SERVER_TABLES` (default OFF) — flip per-view under a live login to activate.
- ⬜ **Activities** has no standalone full-table list view to migrate — activities surface through the calendar feed (already server-windowed via `get_calendar_window` RPC) and per-entity timelines (`getActivitiesForProspect`, indexed). Not the `getAll`-whole-table anti-pattern.
- 🟡 **Import duplicate-preview** (`runDuplicateCheck`, chunks/script-import.js) — the contact path (prospects/customers) no longer needs `getAll(table)` (whole-table download just to dedup an import file). Code wired behind `window.__SERVER_TABLES` to call the **`import_existing_matches`** RPC, which returns ONLY existing rows whose normalized phone/email/ic matches a key present in the import file (normalization mirrors the client `normalisePhone`/lower/strip exactly). Logically equivalent to the whole-table scan (a dup requires key equality), tiny payload. Marketing list types (products/events/promotions) stay on legacy `getAll` (small reference tables). **`import_existing_matches` RPC APPLIED to live 2026-06-14** (additive, whitelisted to prospects|customers). Verified live: normalized-phone lookup finds the row (1), email lookup finds it (1), bogus key + empty arrays match nothing (0/0), and normalization parity holds (client `normalisePhone` pre-normalizes import keys; the RPC normalizes the stored side into the same space). Flag-off + RPC-error both fall back to the byte-identical whole-table path. `node --check` + 6/6 contract green.
- ✅ **Reporting — KPI cards already server-side & live** (pre-existing). `_tryKpiRPCs` (chunks/script-reporting.js) runs UNCONDITIONALLY (no flag) and calls three live `jsonb` RPCs — `kpi_activity_summary`, `kpi_purchase_summary`, `kpi_user_summary` (`p_from,p_to,p_agent_ids,p_role`) — falling back to the 11-call client path on error. Verified live: `kpi_purchase_summary` total_sales = **1,826,696**, exactly matching an independent aggregation, so the shipped RPCs are correct. The 11 KPI-card values (CPS, total/POP/EPP sales+counts, new agents/customers, meetings, EPP details) no longer download purchases/customers/activities whole.
- ✅ **Reporting — Agent leaderboard migrated + BUG FIXED** (`renderAgentLeaderboard`). New **`agent_sales_by_period`** RPC (APPLIED to live 2026-06-14, additive, scope via `p_agent_ids`) sums per-agent current+previous sales in one grouped pass, resolving the agent through `customer.responsible_agent_id`. This fixed a **pre-existing production bug**: the old code matched `p.agent_id === agent.id` but `purchases` has NO `agent_id` column, so the visible leaderboard ALWAYS showed zero / "No agent data". Client tries the RPC first, falls back to a corrected (customer-resolved) client computation — **not** flag-gated, because the fix must always be active. ⚠️ After this branch deploys, the leaderboard will start showing real numbers instead of zeros. Verified live: top agent 83,890; scoped-to-one-agent = 36,070 (cross-matches `kpi_purchase_summary`); single overload; scope param restricts correctly. `node --check` + 6/6 contract green.
- ✅ **Reporting — 4 always-client headcounts now server-side** (branch `upgrade/reporting-backlog`, NOT yet deployed). `activityHeadcount`, `meetUpExistingCount`, `cfHeadcount`, `activeAgents` ran on EVERY reporting render via `getAll('activities')` (+ event_attendees/users) — the last per-render full-table pull. New **`kpi_extended_summary`** RPC (APPLIED to live 2026-06-14, additive) returns all four in one jsonb; `calculateKPIs` tries it first (matching `_tryKpiRPCs`), falls back to the per-metric getters. Uses `users.role_level` for the `isAgent` (3..12) band server-side and replicates the 60-day rolling active-agents window + the attendee entity-resolution exactly. Verified live across unrestricted/scoped/role-filtered (meetup 22=22 / scoped 11=11 / role 3=3, cf 0=0, activity_headcount 40=40 / scoped 21=21, active_agents 13=13). **With this, no reporting render pulls a full table** (cards via `kpi_*`/`get_conversion_rate`, headcounts via `kpi_extended_summary`). `conversionRate` already had its own `get_conversion_rate` RPC.
- ✅ **Reporting — purchase drill-downs DEPLOYED** (SW `crm-v2026-06-14-4`). `buildTotalSalesDetails`/`buildPOPDetails`/`buildEPPCasesDetails` fetch via the **`report_purchase_details`** RPC (period+scope+role purchase rows, agent+customer names joined) instead of `getAll('purchases')+getAll('customers')` per click. Try-RPC-first, original whole-table path as fallback. Behavior-preserving. ⚠️ **Surfaced a pre-existing financial inconsistency** (user decision: leave as-is): 13 null-`date` purchases (RM 177,250, real sales) show in the Total-Sales DRILL-DOWN every period but not in the CARD → card 1,826,696 ≠ drilldown 2,003,946. Real fix = data hygiene (assign dates).
- ✅ **Reporting — activity drill-downs DEPLOYED** (SW `crm-v2026-06-14-5`). `buildCPSDetails`/`buildMeetingsDetails`/`buildClientMeetingsDetails`/`buildMeetUpExistingDetails`/`buildCFHeadcountDetails` fetch via the **`report_activity_details`** RPC (type-set ± title-pattern, period+scope+role, agent+entity names joined); CF headcount groups the CPS rows. Original bodies kept as `_*Legacy` fallbacks. Verified live: CPS 99=99, agent-meetings 24=24, client-meetings 22=22, meet-up 22=22. ⚠️ Pre-existing card-vs-drilldown mismatch on Total Meetings (card 46 = agent+client; drilldown 24 = agent only) — preserved, flagged, not changed (per "leave as-is").
- ✅ **Reporting — tail drill-downs DEPLOYED** (SW `crm-v2026-06-14-6`). `buildNewCustomersDetails`, `buildConversionDetails`, `buildActiveAgentsDetails`, `buildActivityHeadcountDetails` via 4 new RPCs (`report_new_customers`, `report_new_prospects`, `agent_last_activity_60d`, `report_activity_headcount_details`). Try-RPC-first; original bodies kept as `_*Legacy`; active-agents + activity-headcount render extracted to shared helpers. Date parity preserved exactly (customer_since inclusive; created_at excludes to-day). Verified live: new customers 164=164, new prospects 361=361, active agents 13=13, headcount 40=40.
- ✅ **Reporting — Target Overview DEPLOYED.** `renderTargetOverview` no longer `getAll('purchases')`: charts use `purchase_sales_by_day` (daily non-agent-package totals, client buckets week/month/quarter/day), product mix uses `purchase_category_counts` (priority keyword match), quarter-actual uses `kpi_purchase_summary`. Try-RPC-first with getAll fallbacks. Verified: daily totals sum to 1,826,696; category FengShui 7=7.
- ✅ **`exportKPIReport`** needs no migration — it only calls `calculateKPIs` (now fully server-side) + the tiny `quarterly_targets`/`yearly_targets` tables.
- ✅ **Reporting backlog COMPLETE** — no reporting code path pulls a full big table (activities/purchases/customers/prospects/event_attendees) any more; all served by RPCs with client fallbacks. `getAll('users')` (≤22 rows) and the small target tables remain as cheap bounded reads (not scale risks).
- ⬜ Next: **purchases_history / agents lists**, then reporting drill-downs above.
- ⬜ Enablement: flip `window.__SERVER_TABLES = true` per-view via a config flag after each flagged view is verified against live data (with login). (KPI cards + leaderboard are already live, not flag-gated.)

## Phase 2 — Centralize the server seam (#11 BFF, #6 RBAC)  🟡 BFF built + deployed

**#11 BFF — IMPLEMENTED + DEPLOYED 2026-06-14** (endpoint live; activation pending one env var).
- `api/customers.mjs` is a real Vercel serverless function (web `Request→Response`, pure `fetch`, zero npm deps): (1) verifies the caller's Supabase JWT via `/auth/v1/user`, (2) computes the visible agent-id scope SERVER-SIDE via the **`bff_visible_agent_ids`** RPC (applied live; mirrors `getVisibleUserIds` using `role_level`), (3) runs a scoped + searched + keyset-paginated query with the SECRET key (bypasses RLS → RLS becomes defense-in-depth). Returns `{ rows, nextCursor }`.
- Secret handling: `SUPABASE_SECRET_KEY` is read ONLY from `process.env` (no default) — **never committed**. URL + publishable key are public defaults. Until the env var is set the endpoint returns **503 `not_configured`** (no data exposure). `api/` removed from `.vercelignore`; tsc clean (`types/node-shim.d.ts` shims `process`).
- `lib/api-client.js` — browser adapter (`window.ApiClient.getCustomers`), attaches the session token. Built, not yet loaded.
- **Activation (user)**: set `SUPABASE_SECRET_KEY` in Vercel env → verify `GET /api/customers` with a bearer token → then load the adapter + flip `renderCustomersTable`'s server path to keyset pagination via `ApiClient`. (Reads keep working via the current `queryAdvanced` path until flipped.)

**#6 RBAC** — `role_level` applied + live (used by the BFF scope RPC). Optional follow-up: a `custom_access_token_hook` to stamp `role_level` into the JWT so RLS reads it without re-deriving; then drop client-side `_getUserLevel` parsing (display-only).

---
### (original Phase 2 notes)  🟡
- 🟡 **#11 BFF** — `api/customers.mjs` Vercel serverless scaffold (browser → `/api` → Postgres; scoping + service role server-side; RLS becomes defense-in-depth). Helpers are `not_wired` stubs (typed). Needs: Vercel `/api` provisioning, `SUPABASE_SERVICE_ROLE_KEY` env (server-only), a `lib/api-client.js` adapter, Upstash rate-limit.
- ✅ **#6 RBAC — `role_level` migration APPLIED to live 2026-06-14** (staged: add column + backfill → verified 0 nulls / 0 out-of-range → NOT NULL + check(1–15) + index). All 22 users backfilled correctly from `"Level N"` strings (Level 1–11 present). Authoritative numeric `users.role_level` now exists server-side. **Still client-display-only** — no read path uses it yet. Follow-up migration: `custom_access_token_hook` to stamp `role_level` into the JWT so RLS reads it directly; then deprecate client-side `_getUserLevel` parsing (display-only).
- ⬜ Sequence: apply migration (dual-read) → wire BFF reads with server-side scope → flip source of truth → drop string parsing.

## Phase 3 — Cut the bespoke complexity (#8 sync, #7 offline)  ⬜
*Do after Phase 1 — server pagination is what makes most of this unnecessary.*
- ⬜ **#8** — once reads are server-paginated, delete the delta-cursor/tombstone/full-reconcile machinery for paginated views; keep a small page+reference cache. When the framework lands (P4), replace the whole layer with **React Query**. The `data.js` contract tests guard this teardown.
- ⬜ **#7** — keep the offline write-queue only for the narrow field path that needs it (agents logging activities offline); drop it elsewhere. Keep the SW asset cache + cache-version reload (works well).

## Phase 4 — Modernize the client (#14 types, #13 framework, #15 maintainability, #9 build)  🟡 #14 done
- ✅ **#14 Type safety** — `tsconfig` (checkJs) + `types/crm.d.ts` (domain rows + `AppDataStore`/`queryAdvanced`/`window.*`). New code is type-checked; expand coverage file-by-file with `// @ts-check`.
- 🟡 **#13 Framework — Phase 4.0 beachhead DEPLOYED + verified live (2026-06-14, SW -11).** React 19 + Vite added as devDeps; `src/react/main.jsx` → `react-dist/react-island.js` (self-contained IIFE, 194 KB / 60 KB gzip). Verified in prod: the island mounts into `#react-island-root` (`__REACT_ISLAND_MOUNTED`, React 19, 1 child rendered) with the vanilla `window.app` shell fully intact. **Opt-in** (`?react=1` or localStorage `crm_react_island=1`) so the React bundle stays off normal users' loads until a real view is migrated. Build is ADDITIVE: Vercel `buildCommand` = `node build.mjs && (npx vite build || echo skipped)` — the vite step is **non-fatal** (committed `react-dist` is the fallback), `build.mjs` untouched. **Gotcha fixed:** Vite lib mode doesn't replace `process.env.NODE_ENV` → `define`d it (also tree-shook React dev code, 587→194 KB).
  - ✅ **4.1 DONE + deployed + verified (SW -12):** `@tanstack/react-query` + `src/react/data/useCustomers.js` (useQuery over the BFF). The opt-in island fetched + rendered **"164 customers via React Query + BFF"** in prod (`__REACT_ISLAND_STATE='ready'`). React 19 + TanStack Query + BFF proven end-to-end. (BFF cold fetch ~3.4s — perf item.)
  - ✅ **4.2 DONE + deployed + verified live (SW -14):** the real **Customers table** renders via a React island. `renderCustomersTable` (chunks/script-prospects.js) delegates to `window.CRMReact.mountCustomersTable(#customers-react-root, {params,meta,pageSize,onNavigate})` when the opt-in island bundle is loaded AND the flag is on (`?react=1` / localStorage / `window.__REACT_CUSTOMERS===true`); the legacy `#customers-table-container` is hidden then (visibility swap). Same eligibility as the BFF branch — unsupported filters (Regular/deficiency/minEvents/purchase) / house-audit / Agent-Eligible fall back to the legacy DOM. `src/react/views/CustomersTable.jsx` reproduces the already-live BFF render (row template, reassign `<select>`, health badge via `window.app.renderQuickHealthBadge`, pagination); data via `useCustomers` (React Query → BFF, `keepPreviousData`); row actions call `window.app.*`. **Verified logged-in (`?react=1`): 164 scoped customers, 50/page, Next→page 2 works, zero console errors.** Opt-in only — normal users never load the bundle.
    - ⚠️ **Build gotcha (cost a cycle): the island MUST build at `target: 'es2022'`, not `es2020`.** esbuild's es2020 *lowering* of React Query v5's class private fields/methods + the minify syntax pass emits a broken `Object.defineProperty(obj,key,null)` in `new QueryObserver` → "Property description must be an object: null" at `useState` → crashes the whole island. Verified matrix: es2020+min crashes; es2020 non-min / es2022+min / esnext+min all clean. (Diagnose via `npx vite build --minify false` + a local harness.) Same `defineProperty` signature as the pre-existing minified `renderApprovalQueue` crash.
  - 🟡 **4.3 data foundation DONE + deployed + verified (no SW bump — island bundle unchanged):** `api/prospects.mjs` (mirrors api/customers.mjs: JWT verify → `bff_visible_agent_ids` scope → SECURITY-DEFINER `prospects_page` RPC via SECRET key → `{rows,count}`; dormancy + search + ming_gua/agent + sort + page in one round-trip) + `src/react/data/useProspects.js` (useQuery, keepPreviousData; not yet bundled). **Verified live logged-in: `/api/prospects?limit=5&sort=score` → 200, count 362 scoped active prospects, sorted desc.**
    - ✅ **4.3 UI island DONE + deployed + verified live (SW -15):** the Prospects TABLE renders via a React island (`src/react/views/ProspectsTable.jsx`) on `useProspects`. `renderProspectsTable` delegates to `window.CRMReact.mountProspectsTable` when opt-in bundle loaded + flag on + server-eligible (`!_pUnsupported`); card view + derived filters / protection sort → legacy. Faithful row template; row helpers/actions/sort/selection all call `window.app.*` (already exported). Selection = React-local Set seeded per-mount (via `key`) + synced to `_selectedProspects` through `app.toggleProspectSelect` (bulk bar + bulk actions work). Stats written into `#prospect-stats-row` (Total = BFF count; High/Active/Avg page-based, marked `*`). **Verified: 362 scoped, pagination (page 2: 51–100 of 8), bulk checkbox→bulk bar "1", name-sort re-sorts + resets page, reassign dropdowns, zero console errors.** Minor opt-in-only edge: a one-time `count 0` during the SW -14→-15 activation reload (React Query raced the session, cached empty 30s); self-corrected, didn't recur on clean reload — documented, not fixed (proportionate).
  - ✅ **PROMOTED TO DEFAULT — LIVE FOR ALL USERS (SW -16, user-authorized):** Customers + Prospects React screens render by default for the whole team (no longer opt-in). Kill-switch: `?react=0` or `localStorage crm_react_off=1` (also stops the bundle loading); legacy auto-fallback on error. Verified live both ways (clean URL → React 362/164; `?react=0` → legacy). Runtime-verified as admin; agent scope verified by code parity (no over-exposure). Next risk-down: runtime-check one agent account.
  - ⬜ **Then (4.4):** reports → rest. Delete per-view sync (Phase 3) as each migrates; retire `window.app` + consolidate (Next.js or stay Vite) once most views are React.
- ⬜ **#15 Maintainability** — replace the `window.app` god-object + inline `onclick="app.fn()"` with typed modules + component handlers as views migrate. Interim: split the largest chunks (prospects 606 KB, calendar, marketing) into feature modules.
- 🟡 **#9 Build** — Vite now runs alongside `build.mjs` in the Vercel buildCommand (additive, non-fatal). End state: once most views are React islands, retire `build.mjs` and let Vite own the build (tree-shaking, HMR, code-split); stop committing hashed bundles. For now both coexist (legacy esbuild bundles + the Vite island bundle).

---

## This session delivered (on `upgrade/phases-6-15`, NOT pushed)
| Item | Status | Verified by |
|------|--------|-------------|
| #10 tests/CI | ✅ | `node ci/test.mjs` (tsc + 6/6 contract) green |
| #12 customers pagination | ✅ first slice | `queryAdvanced` paginates live; flag-off = legacy; regression green |
| #14 TypeScript infra | ✅ | `tsc --noEmit` exit 0 |
| #11 BFF / #6 RBAC | 🟡 scaffold | `api/customers.mjs`, `role_level` migration (typed, not wired) |
| #8 sync / #7 offline / #13 / #15 / #9 | ⬜ planned | specced above |

Run locally: `node ci/test.mjs` · `node ci/regression.js` · (e2e) `npx playwright test`.
